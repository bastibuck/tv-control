{
  "name": "tv-control",
  "private": true,
  "volta": {
    "node": "25.9.0",
    "pnpm": "10.33.2"
  },
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm --filter @tv-control/server start",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format",
    "formatcheck": "pnpm -r formatcheck"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
